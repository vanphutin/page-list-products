import React from "react";

const Feature = () => {
  return (
    <div style={{ with: "100%" }}>
      <div className="sort">
        Sort by price <button>UP</button> <button>DOWM</button>
      </div>
    </div>
  );
};

export default Feature;
